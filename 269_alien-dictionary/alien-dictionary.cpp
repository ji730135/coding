#include <vector>
#include <string>
#include <stack>
using namespace std;

class Solution
{
public:
    string alienOrder(vector<string> &words)
    {
        for (auto str : words)
        {
            for (int i = 0; i < str.length(); i++)
            {

            }
        }
    }
};